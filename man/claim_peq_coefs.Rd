% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis-helpers.R
\name{claim_peq_coefs}
\alias{claim_peq_coefs}
\title{Build a \code{.contrasts}-shaped list from a \verb{\%=\%} chain}
\usage{
claim_peq_coefs(claim, processed, operand_resolver = peq_operand_as_param)
}
\arguments{
\item{claim}{A \code{null_claim} object with \code{op == "\%=\%"}.}

\item{processed}{The processed model output (\code{.proc} / \code{lazy@processed}),
used to look up the grouping variable name.}

\item{operand_resolver}{A function \verb{function(node)} that turns a single
\verb{\%=\%} operand into a \code{claim_contrast_coefs()}-shaped list (\code{coefs},
\code{scalar}, \code{op}). Defaults to \code{\link[=peq_operand_as_param]{peq_operand_as_param()}}, which treats the
operand as a single parameter equated to zero — the current behavior
for \code{MU(a) \%=\% (2 * MU(b)) \%=\% MU(c)}. Supply a different resolver to
support other \verb{\%=\%} notations (e.g. operands that are themselves linear
combinations, \code{mu1 - mu2 = mu2 + mu3 = ...}).}
}
\value{
A named list with one element: a numeric matrix with \code{"ops"} and
\code{"scalars"} attributes.
}
\description{
Extracts coefficients from a \verb{\%=\%}-chained claim, e.g.
\code{MU(a) \%=\% (2 * MU(b)) \%=\% MU(c)}. Each operand becomes its own column
(\code{h01}, \code{h02}, ...). The matrix carries \code{"ops"} and \code{"scalars"}
attributes, and is returned inside a list named by the model's grouping
variable — the exact shape \code{.contrasts} resolvers return.
}
