% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis-helpers.R
\name{claim_list_h0_coefs}
\alias{claim_list_h0_coefs}
\title{Build a \code{.contrasts}-shaped list from a \code{list_h0()} block}
\usage{
claim_list_h0_coefs(claim, processed)
}
\arguments{
\item{claim}{A \code{list_h0_claims} object, as returned inside \code{\link[=state_null]{state_null()}}
when using \code{\link[=list_h0]{list_h0()}}.}

\item{processed}{The processed model output (\code{.proc} / \code{lazy@processed}),
used to look up the grouping variable name.}
}
\value{
A named list with one element: a numeric matrix with \code{"ops"} and
\code{"scalars"} attributes.
}
\description{
Extracts each named claim's contrast coefficients via
\code{\link[=claim_contrast_coefs]{claim_contrast_coefs()}} and assembles them into a single matrix, one
column per named hypothesis, with each claim's own operator and scalar.
Rows are ordered by \code{.base_null} if present, otherwise by first
appearance. The matrix carries \code{"ops"} and \code{"scalars"} attributes, and is
returned inside a list named by the model's grouping variable — the exact
shape \code{.contrasts} resolvers return.
}
