% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis-helpers.R
\name{peq_operand_as_param}
\alias{peq_operand_as_param}
\title{Default \verb{\%=\%} operand resolver: a single parameter equated to zero}
\usage{
peq_operand_as_param(node)
}
\arguments{
\item{node}{A single \verb{\%=\%} operand node (a \code{param_obj} or \code{arith_node}).}
}
\value{
A \code{claim_contrast_coefs()}-shaped list (\code{coefs}, \code{scalar}, \code{op}).
}
\description{
Treats a \verb{\%=\%} operand (e.g. \code{MU(a)} or \code{2 * MU(b)}) as \verb{<operand> == 0}
and extracts its coefficient via \code{\link[=claim_contrast_coefs]{claim_contrast_coefs()}}. This is the
default \code{operand_resolver} for \code{\link[=claim_peq_coefs]{claim_peq_coefs()}}, matching
\code{MU(a) \%=\% (2 * MU(b)) \%=\% MU(c)}-style chains.
}
\details{
Default \verb{\%=\%} operand resolver: a single parameter equated to zero

Treats a \verb{\%=\%} operand (e.g. \code{MU(a)} or \code{2 * MU(b)}) as \verb{<operand> == 0}
and extracts its coefficient via \code{\link[=claim_contrast_coefs]{claim_contrast_coefs()}}. This is the
default \code{operand_resolver} for \code{\link[=claim_peq_coefs]{claim_peq_coefs()}}, matching
\code{MU(a) \%=\% (2 * MU(b)) \%=\% MU(c)}-style chains.
}
\keyword{internal}
